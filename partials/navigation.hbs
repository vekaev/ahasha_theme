<ul>
    <li class="{{link_class for=('/') class=(concat "nav-" slug)}}"><a href="/">all</a></li>
    {{#foreach navigation}}
    <li class="{{link_class for=(url) class=(concat "nav-" slug)}}"><a href="{{url absolute="true"}}">{{label}}</a></li>
    {{/foreach}}
</ul>